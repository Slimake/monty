#include <stdio.h>
#include <stdlib.h>
#include "monty.h"

/**
 * main - entry point into the program
 * @argc: command-line argument counter
 * @argv: array of pointers(an array containing command-line arguments)
 *
 * Return: an integer
 */

int main(int argc, char *argv[])
{
	monty(argc, argv);

	return (0);
}
